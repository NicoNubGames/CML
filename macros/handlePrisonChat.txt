// Is our inventory full?

STRIP(&chat,%CHAT%)

IF(!@stop)
	IFMATCHES(%&chat%,^Your inventory.*)
		PLAYSOUND(custom.good)
		SET(@stop)
	ENDIF

	IFMATCHES(%&chat%,^Hey.*)
		PLAYSOUND(custom.problem)
		SET(@stop)
	ENDIF
ENDIF